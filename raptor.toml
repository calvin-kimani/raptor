# Raptor Framework Configuration
# This is the default configuration for the Raptor installation

[raptor]
version = "0.1.0"
installation_path = "/home/user/.raptor"  # Will be set during installation

[schemas]
# Default schema directories (relative to raptor installation)
findings = "schemas/findings"
reports = "schemas/reports"

# Additional custom schema directories
# Format: "type=path1:path2:path3" where type is findings/reports
# Multiple paths can be separated by colons (:)
# Can be absolute paths or relative to raptor installation
additional_dirs = [
    # "findings=~/custom-finding-schemas",
    # "reports=~/custom-report-schemas:~/team-report-schemas"
]

[scripts]
# Default scripts directory (relative to raptor installation)
default_dir = "scripts"

# Additional custom script directories
# Format: "type=path1:path2:path3" where type identifies the script category
# Multiple paths can be separated by colons (:)
# Can be absolute paths or relative to raptor installation
additional_dirs = [
    # "analysis=~/my-analysis-scripts",
    # "reporting=~/custom-reports",
    # "utilities=~/audit-utils:~/team-utils"
]

[defaults]
# Default settings for new projects
test_framework = "foundry"  # foundry, hardhat, or custom
severity_levels = ["LOW", "MEDIUM", "HIGH", "CRITICAL"]
report_formats = ["sherlock", "code4rena", "codehawks"]
